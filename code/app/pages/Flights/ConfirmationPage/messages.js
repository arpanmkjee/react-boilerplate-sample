/*
 * 
 *
 * 
 */
import { defineMessages } from 'react-intl';

export const scope = '';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Page not found.',
  },
});
